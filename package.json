{
  "name": "kittyui-monorepo",
  "private": true,
  "version": "0.0.0",
  "description": "A Rust core + React bindings framework for building rich terminal apps using the Kitty graphics protocol",
  "workspaces": ["packages/*"],
  "scripts": {
    "build": "bun run --filter '*' build",
    "test": "bun run test:rs && bun run test:ts",
    "test:rs": "cargo test --manifest-path packages/core-rust/Cargo.toml",
    "test:ts": "bun run --filter '*' test",
    "clean": "bun run --filter '*' clean",
    "lint": "bun run lint:rs && bun run lint:ts",
    "lint:rs": "cargo clippy --manifest-path packages/core-rust/Cargo.toml -- -D warnings",
    "lint:ts": "oxlint packages/core packages/react packages/create-kittyui",
    "fmt": "bun run fmt:rs && bun run fmt:ts",
    "fmt:rs": "cargo fmt --manifest-path packages/core-rust/Cargo.toml",
    "fmt:ts": "oxfmt --write packages/core packages/react packages/create-kittyui",
    "fmt:check": "cargo fmt --manifest-path packages/core-rust/Cargo.toml --check && oxfmt --check packages/core packages/react packages/create-kittyui",
    "typecheck": "bun run --filter '*' typecheck",
    "check": "bun run lint && bun run typecheck"
  },
  "devDependencies": {
    "oxfmt": "^0.40.0",
    "oxlint": "^1.55.0"
  }
}
