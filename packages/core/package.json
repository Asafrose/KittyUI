{
  "name": "@kittyui/core",
  "version": "0.1.0",
  "description": "Core TypeScript bindings for KittyUI",
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "bun run build:native && tsc --build",
    "build:native": "bash scripts/build-native.sh",
    "clean": "rm -rf dist native",
    "test": "bun test",
    "typecheck": "tsc --noEmit"
  },
  "files": [
    "dist"
  ]
}
