/**
 * @kittyui/core — Core TypeScript bindings for the KittyUI rendering engine.
 *
 * Native Rust bindings will be wired up in issue #2.
 */

export function hello(): string {
  return "Hello from @kittyui/core!";
}
